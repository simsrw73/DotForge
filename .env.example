# Copy to .env (gitignored) and fill in. The pipeline/release tooling reads
# these EXACT names (case-insensitive match, but keep the underscores).
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
PSGALLERY_API_KEY=
